<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= raw(@article.description) %>
</p>

<p>
  <strong>Tags:</strong>
   <%= @article.tags.empty? ? 'no tags present' : @article.tags.map{|t|t.name}.join(', ')%>
</p>  




<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
